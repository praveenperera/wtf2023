# Agent Instructions

- Do not use hand-drawn SVGs when Recharts or lucide can cover the need
- Always prefer ready-made components
- Always use existing shadcn components when available
- Public-facing copy must read as final site content, not implementation or migration notes. Do not mention preservation, legacy status, CMS/source migration, original pages, local assets, first-party assets, redirects, screenshots being copied, memes, receipts, or why content was restored unless the user explicitly asks for that wording. Use neutral article-native labels such as "Article images" or omit labels entirely.
